spring:
  application:
    name: discovery-server

server:
  port: ${PORT}

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false


management:
  server:
    port: ${MANAGEMENT_PORT}
  endpoints:
    web:
      exposure:
        include: health, info